<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== Permisos ===== -->
    <!-- Acceso a internet: necesario para Firebase y Google Maps. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Ubicación: para la geolocalización con Google Maps (Paso 3). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="Gestor de Tareas"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher">

        <!-- ===== Clave de la API de Google Maps =====
             IMPORTANTE: reemplaza "TU_API_KEY_DE_GOOGLE_MAPS" por tu clave real
             obtenida en Google Cloud Console (Maps SDK for Android habilitado). -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC--VYWlZ5rjF1ehY0mPTK-JQJbTw7_DJw" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Tema de arranque mientras Flutter inicializa. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Permite consultar el navegador/servicios necesarios (Android 11+). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
